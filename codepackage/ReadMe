


Files
-----

instrutil.h and instrutil.c:
   contain helper functions for code generation

parse.y:
  computer-readable grammar rules for the language

testcases/
  contains basic test cases, sample code input

iloc.out
	When you run "codegen", the generated iloc code should be in
"iloc.out"

Makefile:
  Run "make" to produce "codegen" which will generate the iloc
code according to the input code files



